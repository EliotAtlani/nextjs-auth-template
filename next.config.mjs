/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "github.com",
      "lh3.googleusercontent.com",
      "image.s3.amazonaws.com",
    ],
  },
};

export default nextConfig;
